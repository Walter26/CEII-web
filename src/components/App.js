import React from 'react';
import {BrowserRouter } from 'react-router-dom';

import Layout from './Layout';

var App = () => {
    return(
        <BrowserRouter>
            <Layout>
                
            </Layout>
        </BrowserRouter>
    );
}

export default App;